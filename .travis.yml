os: linux
dist: xenial
language: node_js
node_js: 12
install:
  - npm install
script:
  - npm run build
  - npm run serve &
  - npm test

jobs:
  include:
    - stage: release
      deploy:
        provider: script
        script:
          - npm run semantic-release
